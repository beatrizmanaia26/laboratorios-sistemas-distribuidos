copiar codigos do gitlab:https://gitlab.com/laferreira/fei/cc7261/-/blob/main/rpc/lab.md
para rodar, entrar no src
roda docker-compose up -build (forca a fazer build o compose)
imagem dessa semana compila protobuf

---
extra:cliente acha q roda local mas ta rodando no servidor

quando mudo dockerfile tenhp que rodar o docker-compose up --build p recriar a imagem


--esse lab:
Implementa cliente e servidor usando protonuf 
Na aula cliente servidor cliente manda helo servidor manda Orlando e ocorre o cliente chama China o q roda no servidor

Agr tem várias mensagem do ciente pro segvudorclientepodemanda 4/5 funcoesrposevudor s servidores torna resultado p executa funco s 
Faço build da classe q servidor implementa e build do drive q cliente chama no protobuf 
E o extra temos o cliente o s1 q cliente fala rest com s1 e s1 fala com s2 por rpc

--explicacao:
teoria: so fizemos hello world, criamos protobuf que mandava mensagem e recebia outra, nesse o protobuf tem todo o crud de tarefas e troca de mensagem (envia e recebe algo devolta)
faco implementacao das funcoes deo cliente e servidor  usando protobuf

na aula cliente manda hello e servidor retorna world, isso ocorre pq cliente chama funcao q eh executada no servidor
agr n tem so 1 mensagem que envia e recebe, a gnt recebe varias 
agora cliente manda mensagem pro servidor pode manda 4 ou 5 funcoes, crud mais 1 conexao, e servidor retorna resultado p executar funoes (base segunda com mais coisa)
mais funcoes e mais emnsagens p envia, protobuf faz build da classe que servidor implementa e build do stub que cliente chama

 ai a parte extra a gnt fez 
o cliente manda rest pro s1 e s2 manda rpc pro s2, cliente acha que ta local mas n ta 